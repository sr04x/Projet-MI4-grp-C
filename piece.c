#include "piece.h"

void lire_piece(char* nom_fichier,Piece* piece){
  FILE* fichier= fopen ("style_piece.txt","r");
  if (fichier==NULL){
    printf("erreur dans la lecture du fichier\n");
  return;
  }
}
fget(fichier,piece->nom,taille_nom);
for(int i=0; i<taille_piece,i++){
  fget(fichier,piece->forme[i],taille_piece);
} 
